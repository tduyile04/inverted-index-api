const expect = require('expect');

describe('Inverted index test', () => {
  describe('Case for reading books data', () => {
    it('should return "invalid" if the file content is not a valid JSON array', () => {
      expect(2).toEqual(2);
    });
  });
});
